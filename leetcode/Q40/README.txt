time complexity: 
DFS(depth first seach:) If depth is n(actually n here is size of Candidates in the worst case), then the time
complexity is 2^n.

space complexity:
kn(k is the breadth, n is the size of Candidates). The space complexity is k times n.
