1: What is binary search?
Reduce the search space by each step:
Inputs usually needs to be sorted.

2:Why Binary search?
Fast
O(n)=O(n/2)+O(eval)
O(log(range))*O(eval) v.s. O(range)*O(eval) linear scan:
O(eval) is time to determine the direction and whether we have reached the solutions.

